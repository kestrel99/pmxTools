% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cut_quantile.R
\name{cut_quantile}
\alias{cut_quantile}
\title{Create quantile-based bins for continuous variables}
\usage{
cut_quantile(
  dat,
  var,
  n_groups = 4,
  missing_codes = c(-99, -999),
  blq_label = "BLQ",
  unit = NULL,
  id = NULL,
  verbose = FALSE
)
}
\arguments{
\item{dat}{A data frame containing the variables to bin.}

\item{var}{Variable(s) to bin: single name, character vector, or named list.
Named list allows different quantile cuts per variable, e.g.
\code{list(AGE = c(4, 3), WT = 4)} creates both quartiles and tertiles for AGE.}

\item{n_groups}{Number of quantile groups (2-5). Can be:
\itemize{
\item Single value applied to all variables
\item Vector applied in order
\item Named list for per-variable settings
}}

\item{missing_codes}{Values to treat as missing (defaults to c(-99, -999)).}

\item{blq_label}{Label for BLQ/zero values (defaults to "BLQ").}

\item{unit}{Unit for display, appended to interval labels. Can be single
value or named list per variable.}

\item{id}{Optional subject ID column. If provided, quantile calculation
uses 1 row per subject. Stops if a subject has conflicting values.}

\item{verbose}{If TRUE, prints summary and returns list with data and summary.}
}
\value{
If \code{verbose = FALSE} (default): returns modified data frame invisibly.
If \code{verbose = TRUE}: returns a list with:
\itemize{
\item \code{data}: modified data frame
\item \code{summary}: tibble with cut details per variable
\item \code{skipped}: tibble of any skipped cuts (due to zero-range bins)
}

Output columns added (for var = "CONC", n_groups = 4):
\itemize{
\item \code{CONCQ4Q}: numeric factor (1, 2, 3, 4)
\item \code{CONCQ4C}: character ("Q1", "Q2", "Q3", "Q4", "BLQ")
\item \code{CONCQ4CC}: continuous factor with intervals
}
}
\description{
Create quantile-based bins for continuous variables
}
\examples{
\dontrun{
# Single variable, quartiles
dat <- cut_quantile(dat, "AGE", n_groups = 4)

# Multiple cuts on same variable
dat <- cut_quantile(dat, "AGE", n_groups = c(4, 3))

# With longitudinal data
dat <- cut_quantile(dat, list(CONC = 4, AGE = 4), id = "SUBJID")

# Verbose output
result <- cut_quantile(dat, list(CONC = c(4, 3), AGE = 4), verbose = TRUE)
}

}
